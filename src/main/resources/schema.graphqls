type Query {
    bookById(id: ID): Book
}

type Book {
    id: ID
    name: String
    pageCount: Int
    author: [Author]
}

type Author {
    id: ID
    firstName: String
    lastName: String
    book: [Book]
    hobbies(type: String): [Hobbies]
}

type Hobbies {
    id: ID
    name: String
}
